{% extends 'base.html.twig' %}

{% block body %}
    <h1>Usuarios list</h1>

    <table>
        <thead>
            <tr>
                <th>Clave</th>
                <th>Nombreusuario</th>
                <th>Nombre</th>
                <th>Apellido</th>
                <th>Status</th>
                <th>Fechacreacion</th>
                <th>Fechaultimoingreso</th>
                <th>Tipo</th>
                <th>Comentario</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for usuario in usuarios %}
            <tr>
                <td><a href="{{ path('usuario_show', { 'id': usuario.id }) }}">{{ usuario.clave }}</a></td>
                <td>{{ usuario.nombreUsuario }}</td>
                <td>{{ usuario.nombre }}</td>
                <td>{{ usuario.apellido }}</td>
                <td>{{ usuario.status }}</td>
                <td>{{ usuario.fechaCreacion }}</td>
                <td>{{ usuario.fechaUltimoIngreso }}</td>
                <td>{{ usuario.tipo }}</td>
                <td>{{ usuario.comentario }}</td>
                <td>{{ usuario.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('usuario_show', { 'id': usuario.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('usuario_edit', { 'id': usuario.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('usuario_new') }}">Create a new usuario</a>
        </li>
    </ul>
{% endblock %}
